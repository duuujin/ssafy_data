# ✍️ Pandas & 기초 통계 시험 (50문항)

### [Part 1: Pandas & NumPy 기초]
1. Pandas의 1차원 데이터 구조 명칭은?
2. Pandas의 2차원 데이터 구조 명칭은?
3. DataFrame의 각 행을 식별하는 고유한 값은?
4. CSV 파일을 DataFrame으로 읽어오는 함수는?
5. 데이터의 컬럼 정보와 Null 개수를 확인하는 함수는?
6. 수치형 데이터의 요약 통계량을 보여주는 함수는?
7. `df.shape` 결과가 `(100, 5)`라면 컬럼(열)의 개수는?
8. 데이터의 상위 5개 행을 확인하는 함수는?
9. NumPy의 다차원 배열 객체 이름은?
10. 특정 컬럼의 값별 개수를 세는 함수는?

### [Part 2: 인덱싱과 필터링]
11. 명칭(Label) 기반 인덱싱 함수는?
12. 위치 정수(Integer) 기반 인덱싱 함수는?
13. `df.loc[0:3]`은 인덱스 3번을 포함하는가?
14. `df.iloc[0:3]`은 3번째 행을 포함하는가?
15. 'Age'가 30보다 큰 행을 필터링하는 코드를 작성하시오.
16. 여러 조건을 동시에 만족(AND)할 때 쓰는 기호는?
17. 여러 조건 중 하나만 만족(OR)해도 될 때 쓰는 기호는?
18. 특정 열('City')을 삭제할 때 사용하는 함수는?
19. `axis=1`은 행과 열 중 무엇을 의미하는가?
20. `inplace=True` 인자의 목적은 무엇인가?

### [Part 3: 데이터 전처리]
21. 결측치(NaN)가 있는지 확인하는 함수는?
22. 결측치를 0으로 채우는 코드는?
23. 결측치가 포함된 행을 제거하는 함수는?
24. 'Score' 열을 기준으로 내림차순 정렬하는 함수와 인자는?
25. 중복된 행을 제거하는 함수는?
26. 컬럼 이름을 바꿀 때 쓰는 함수는?
27. 행과 열을 뒤바꾸는(전치) 속성은?
28. 복잡한 함수를 데이터에 일괄 적용하는 함수는?
29. 이름이 없는 익명 함수를 무엇이라 하는가?
30. 특정 컬럼의 데이터를 다른 값으로 교체하는 함수는?

### [Part 4: 그룹화 및 병합]
31. 특정 컬럼별로 데이터를 묶는 함수는?
32. `groupby` 이후 여러 집계(mean, sum)를 한 번에 하는 함수는?
33. 공통 키(Key)를 기준으로 두 테이블을 합치는 함수는?
34. 두 데이터를 단순히 이어 붙이는 함수는?
35. `merge` 시 교집합 데이터만 남기는 방식의 이름은?
36. 넓은 데이터를 긴 형태로 녹이는 함수는?
37. 긴 데이터를 넓은 표 형태로 변환하는 함수는?
38. `concat` 시 열 방향으로 합치기 위한 `axis` 값은?
39. 그룹화된 데이터의 합계를 구하는 메서드는?
40. `df.sort_index()`는 무엇을 기준으로 정렬하는가?

### [Part 5: 기초 통계]
41. 전체 집단(모집단)을 다 조사하기 어려워 뽑은 집단은?
42. 표본이 모집단을 대표하지 못하고 왜곡되는 현상은?
43. 표본 크기가 커지면 평균의 분포가 정규분포를 따른다는 정리는?
44. "차이가 없다"는 기본 가설의 이름은?
45. 증명하고 싶은 "차이가 있다"는 가설의 이름은?
46. 유의확률을 나타내는 용어는?
47. 통상적으로 p-value가 얼마보다 작아야 유의미하다고 보는가?
48. 두 집단의 평균 차이를 검정하는 방법은?
49. 정규분포에서 약 95% 데이터가 들어있는 구간은 평균 ± (  ) 표준편차이다.
50. 가설 검정에서 귀무가설을 기각하게 되는 영역은?

---

## 🔑 정답지

1. Series / 2. DataFrame / 3. Index / 4. read_csv() / 5. info() / 6. describe() / 7. 5개 / 8. head() / 9. ndarray / 10. value_counts()
11. loc / 12. iloc / 13. 예 / 14. 아니오 (2번 인덱스까지만) / 15. df[df['Age'] > 30] / 16. & / 17. | / 18. drop() / 19. 열(Column) / 20. 원본 데이터 바로 변경
21. isna() / 22. fillna(0) / 23. dropna() / 24. sort_values(by='Score', ascending=False) / 25. drop_duplicates() / 26. rename() / 27. T (또는 transpose) / 28. apply() / 29. lambda / 30. replace()
31. groupby() / 32. agg() / 33. merge() / 34. concat() / 35. inner join / 36. melt() / 37. pivot() / 38. axis=1 / 39. sum() / 40. 인덱스(Index)
41. 표본(Sample) / 42. 표본 편향 / 43. 중심극한정리 / 44. 귀무가설 / 45. 대립가설 / 46. p-value / 47. 0.05 / 48. t-test / 49. 2 (정확히는 1.96) / 50. 기각역